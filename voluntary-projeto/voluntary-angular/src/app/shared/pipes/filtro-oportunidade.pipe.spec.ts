import { FiltroOportunidadePipe } from './filtro-oportunidade.pipe';

describe('FiltroOportunidadePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroOportunidadePipe();
    expect(pipe).toBeTruthy();
  });
});
